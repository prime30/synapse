/**
 * Built-in tasks barrel -- EPIC F
 *
 * Import this file to register all built-in tasks with the TaskRunner.
 * Each module self-registers as a side effect.
 */

import './cleanup-sessions';
import './warm-embeddings';
import './consolidate-memory';
import './sync-health-check';
